{
  "name": "sift-measure",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "gulp build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "@redsift/sift-sdk-web": "0.6.1",
    "@redsift/redsift-bundler": "0.5.3",
    "gulp": "^3.9.1"
  }
}
